# Interfaces

**Contracts only.** A file in here says *what* something can do, and never *how* it does it.

No logic, no fields, no `new`. If you find yourself writing a line of code that
actually does something, it belongs somewhere else.

This is the only folder you need for Stage 1.
