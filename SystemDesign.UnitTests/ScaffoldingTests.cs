namespace SystemDesign.UnitTests;

/// <summary>
/// Proves the unit test project is wired up correctly. Nothing here tests the
/// application — it tests that you can run tests at all.
///
/// Leave this in place until you've written your first real test, then delete it.
/// </summary>
public class ScaffoldingTests
{
    [Fact]
    public void TheTestProjectRuns()
    {
        Assert.True(true);
    }
}
